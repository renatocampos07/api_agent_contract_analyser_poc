# Example environment variables for the prototype backend
# Copy this file to .env and fill with real values (do NOT commit real secrets)

# Environment
ENVIRONMENT=development
APP_HOST=0.0.0.0
APP_PORT=8000

# Storage / runtime
STORAGE_TYPE=local   # local | s3 | azure
AUTH_TYPE=none       # none | jwt | oauth

# Azure / OpenAI (if using Azure OpenAI)
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_KEY=
AZURE_OPENAI_DEPLOYMENT_NAME=

# Anthropic (optional)
ANTHROPIC_API_KEY=

# Database / persistence
DATABASE_URL=        # e.g. postgresql://user:pass@host:5432/dbname

# Other keys used by the app
# EXAMPLE_KEY=

# SECURITY NOTE: keep secrets out of git. Add this repo's .env to your local
# environment or a secrets manager, and do not commit real credentials.
